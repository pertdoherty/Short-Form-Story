export interface StoryIdea {
    title: string;
    description: string;
    storyline: string;
}

export interface GenerationParams {
    projectName: string;
    concept: string;
    tone: string;
    quantity: number;
}